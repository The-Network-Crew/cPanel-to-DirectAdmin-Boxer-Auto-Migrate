# What is this script?

This exists within DA and is used to convert a cpmove-user.tar.gz file into a DirectAdmin backup file, before it is then restored into the system.

We have included this for reference and to better correlate BOXER output with the behind-the-scenes engine room. It is not modified by us.